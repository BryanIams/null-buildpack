#!/usr/bin/env bash

# fail fast
set -e

# parse args
export BUILD_DIR=$1

BIN_DIR=$(cd $(dirname $0); pwd) # absolute path
. ../../lib/java
. ../../lib/util

# Install JDK
install_java_with_overlay ${BUILD_DIR}