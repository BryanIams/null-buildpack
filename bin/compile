#!/usr/bin/env bash

# fail fast
set -e

# parse args
export BUILD_DIR=$1

BIN_DIR=$(cd $(dirname $0); pwd) # absolute path

echo $BIN_DIR

BIN_ROOT=$(dirname $BIN_DIR)

echo $BIN_ROOT

. $BIN_ROOT/lib/java
. $BIN_ROOT/lib/util

# Install JDK
install_java_with_overlay ${BUILD_DIR}